创建一个静态资源服务器

res是一个双工流

mime 第三方模块可以根据路径返回一个文件结尾
mime.getType(path) 

服务器上不会认文件的

html text/html

静态服务器: 根据路径，找到资源

老师，我一直有一个很low的困惑，很多用户同时访问一个服务器，node是单线程的，他是怎么保证req上的信息对应的某一个用户，获取到数据之后还能通过res给到对应的用户，不会出现用户访问数据混乱的问题，还一个就是node端记住用户的登录态，这个是怎么做到的，koa的ctx.session保存着用户信息，为啥不会混乱呢




还一个就是node端记住用户的登录态，这个是怎么做到的，koa的ctx.session保存着用户信息，为啥不会混乱呢

实际项目里面，每次收到请求都这样计算对比一次，不久耗费性能了


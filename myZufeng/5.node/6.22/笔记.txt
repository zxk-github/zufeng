切片 在前面增加逻辑，在函数执行后执行某些事
vue利用切片重写数组的方法

发布订阅 观察者模式

pomise链式如何中断
1. 返回一个失败的，走到catch
2. 返回一个新的promise，这个promise不成功也不失败
new Promise(() => {})

能不能取消一个promise执行
1. 立即让当前的promise失败，但是promise发起的请求不会中断，仅仅是返回值不接收

es6 
import也会存在变量提升

实现mixin 混合 两个函数混合

子类和父类混合

class中的this问题
不能把类中原型方法单独拿出来执行,此时this是undefined

一个函数被多次绑定之后，函数中的this是第一次绑定的对象

展平数组

es6 模块化

await 本质上就是一个then后面的代码

进程 应用的最小单位

buffer 
实现buffer的split
buf.indexof() 获取索引的 

promise删除文件

O1: 商城首页包管理打开速度优化
K1: 分块加载，解决低配安卓手机渲染缓慢问题
K2: node端数据做redis缓存，减少后端请求

O2: 提高生活作风前端开发效率
k1: 优化现在生活作风编译速度, 提高开发效率

O3: 参与技术网站开发


